import { type Metadata } from "next"

import { TaxRatesSettingsPage } from "@/features/settings/tax-rates/components"

export const metadata: Metadata = {
  title: "Tax Rates — Settings"
}

export default TaxRatesSettingsPage
